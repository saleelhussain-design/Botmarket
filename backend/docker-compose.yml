version: '3.8'
services:
  backend:
    build: .
    container_name: botmarket-backend
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - ./database.sqlite:/app/database.sqlite
    restart: always
